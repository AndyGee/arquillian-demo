# @RunWith(Arquillian.class) - "Production Near" JavaEE Testing

### Summary
This project compliments the @RunWith(Arquillian.class) - "Production Near" JavaEE Testing talk.

To compile and run the project just execute the following commands from a console in this directory:

	mvn clean install


It consists of the following examples:

...